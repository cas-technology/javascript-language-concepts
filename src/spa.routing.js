const routingModule = function () {
    const init = function () {
        this.registerRoutes();
    };

    const registerRoutes = function () {

    };

    const route = function (module) {
        new module;
    };

    return {
        init,
        registerRoutes,
        route,
    }
};
